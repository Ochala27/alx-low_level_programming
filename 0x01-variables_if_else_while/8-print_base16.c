#include <stdio.h>
/**
 * main - Hexadecimal
 * Return: always return 0
 */
int main(void)
{
int e;
int l;
for (e = 48; e <= 57; e++)
{
putchar(e);
}
for (l = 97; l <= 102; l++)
{
putchar(l);
}
putchar('\n');
return (0);
}
